# Required for all users (consumers and admins)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Required only for admin operations (ingestion, writes)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Required for Meta Marketing API tools (optional — knowledge base tools work without it)
# META_ACCESS_TOKEN=your-meta-access-token
